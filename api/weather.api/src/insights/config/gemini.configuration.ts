/*import { registerAs } from "@nestjs/config";

export interface IGeminiApiConfig {
  geminiKEY: string;
  model: string;
  prompt: string;
}

export default registerAs('gemini', (): IGeminiApiConfig => ({
  geminiKEY: process.env.GEMINI.KEY,
  model: process.env.GEMINI_MODEL,
  prompt: process.env.GEMINI_PROMPT,
}));*/
